services:
  mongodb:
    image: mongo:latest
    container_name: dresstyle-mongodb
    ports:
      - "27017:27017"
    networks:
      - dresstyle-network
    volumes:
      - mongo-data:/data/db

  rabbitmq:
    image: rabbitmq:3-management
    container_name: dresstyle-rabbitmq
    environment:
      - RABBITMQ_DEFAULT_USER=admin
      - RABBITMQ_DEFAULT_PASS=admin123
    ports:
      - "5672:5672"
      - "15672:15672"
    networks:
      - dresstyle-network
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "check_running"]
      interval: 10s
      timeout: 5s
      retries: 5

  discovery-server:
    build: ./services/discoveryserver
    container_name: dresstyle-discovery
    ports:
      - "8761:8761"
    networks:
      - dresstyle-network

  api-gateway:
    build: ./services/apigateway
    container_name: dresstyle-gateway
    ports:
      - "8080:8080"
    environment:
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://discovery-server:8761/eureka/
      - JWT_SECRET=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
    depends_on:
      discovery-server:
        condition: service_started
    networks:
      - dresstyle-network

  auth-service:
    build: ./services/authservice
    container_name: dresstyle-auth
    ports:
      - "8081:8081"
    environment:
      - SPRING_DATA_MONGODB_URI=mongodb://mongodb:27017/dresstyle_auth
      - SPRING_RABBITMQ_USERNAME=admin
      - SPRING_RABBITMQ_PASSWORD=admin123
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://discovery-server:8761/eureka/
      - JWT_SECRET=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
      - ADMIN_NAME=admin
      - ADMIN_EMAIL=admin@admin.com
      - ADMIN_PASSWORD=admin
    depends_on:
      mongodb:
        condition: service_started
      discovery-server:
        condition: service_started
      rabbitmq:
        condition: service_healthy
    networks:
      - dresstyle-network

  catalog-service:
    build: ./services/catalogservice
    container_name: dresstyle-catalog
    ports:
      - "8082:8082"
    environment:
      - SPRING_DATA_MONGODB_URI=mongodb://mongodb:27017/dresstyle_catalog
      - SPRING_RABBITMQ_HOST=rabbitmq
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://discovery-server:8761/eureka/
      - JWT_SECRET=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
    depends_on:
      mongodb:
        condition: service_started
      rabbitmq:
        condition: service_healthy
      discovery-server:
        condition: service_started
    networks:
      - dresstyle-network

  order-service:
    build: ./services/orderservice
    container_name: dresstyle-order
    ports:
      - "8085:8085"
    environment:
      - SPRING_DATA_MONGODB_URI=mongodb://mongodb:27017/dresstyle_orders
      - SPRING_RABBITMQ_HOST=rabbitmq
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://discovery-server:8761/eureka/
    depends_on:
      mongodb:
        condition: service_started
      rabbitmq:
        condition: service_healthy
      discovery-server:
        condition: service_started
    networks:
      - dresstyle-network

  subscription-service:
    build: ./services/subscriptionservice
    container_name: dresstyle-subscription
    ports:
      - "8087:8087"
    environment:
      - SPRING_DATA_MONGODB_URI=mongodb://mongodb:27017/dresstyle_subscription
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://discovery-server:8761/eureka/
      - JWT_SECRET=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
    depends_on:
      mongodb:
        condition: service_started
      discovery-server:
        condition: service_started
    networks:
      - dresstyle-network

  notification-service:
    build: ./services/notificationservice
    container_name: dresstyle-notification
    ports:
      - "8084:8080"
    environment:
      - SPRING_RABBITMQ_HOST=rabbitmq
      - SPRING_RABBITMQ_USERNAME=${RABBITMQ_USERNAME:-admin}
      - SPRING_RABBITMQ_PASSWORD=${RABBITMQ_PASSWORD:-admin123}
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://discovery-server:8761/eureka/
      - NOTIFICATION_EMAIL_ENABLED=${NOTIFICATION_EMAIL_ENABLED:-true}
      - SPRING_MAIL_HOST=${SPRING_MAIL_HOST:-smtp.example.com}
      - SPRING_MAIL_PORT=${SPRING_MAIL_PORT:-587}
      - SPRING_MAIL_USERNAME=${SPRING_MAIL_USERNAME:?Please set SPRING_MAIL_USERNAME}
      - SPRING_MAIL_PASSWORD=${SPRING_MAIL_PASSWORD:?Please set SPRING_MAIL_PASSWORD}
      - SPRING_MAIL_SMTP_AUTH=true
      - SPRING_MAIL_STARTTLS_ENABLE=true
      - NOTIFICATION_FROM=${NOTIFICATION_FROM:-noreply@dresstyle.com}
    depends_on:
      rabbitmq:
        condition: service_healthy
      discovery-server:
        condition: service_started
    networks:
      - dresstyle-network

  web-frontend:
    build: ./vue-project 
    container_name: dresstyle-frontend
    ports:
      - "80:80"
    depends_on:
      api-gateway:
        condition: service_started
    networks:
      - dresstyle-network

networks:
  dresstyle-network:
    driver: bridge

volumes:
  mongo-data: